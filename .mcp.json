{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/specification/main/schema/mcp.schema.json",
  "mcpServers": {
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Chain-of-thought reasoning for complex problem solving. Free, no API key needed."
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "Persistent knowledge graph memory across sessions. Free, local storage."
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation lookup for libraries and frameworks. Free, no API key needed."
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp"],
      "description": "Browser automation for E2E testing and web scraping. Free, no API key needed. Requires: npx playwright install chromium"
    }
  },
  "_comments": {
    "github": "To enable GitHub MCP: add GITHUB_PERSONAL_ACCESS_TOKEN env var and uncomment the github section below",
    "tips": "Keep under 8 MCPs enabled to preserve context window. Disable unused ones.",
    "install_playwright": "Run 'npx playwright install chromium' once before using playwright MCP"
  }
}
